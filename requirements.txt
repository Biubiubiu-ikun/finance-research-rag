# 金融研报 RAG —— 服务/检索/聚合/Agent 运行依赖
# （DeepDOC 解析另需重型依赖，见文末；API 问答/聚合/检索不依赖它）

fastapi==0.138.0
uvicorn[standard]==0.49.0
python-multipart==0.0.32          # /ingest 上传 PDF
elasticsearch==8.11.1             # 混合检索(向量kNN + BM25)
sentence-transformers==2.7.0      # bge 嵌入(领域微调) + reranker；5.x 与 transformers4.46 冲突，锁 2.7
transformers==4.46.1
torch==2.8.0                      # GPU 轮子带 +cuXXX；CPU 部署改用 CPU 版 torch
jieba==0.42.1                     # BM25 中文分词
pdfplumber==0.11.10               # 无边框密集财务表抽取
requests==2.33.1                  # DeepSeek API
numpy==2.1.3
streamlit==1.58.0                 # Demo UI（可选，纯 API 部署可不装）

# —— DeepDOC 布局解析(parse_report.py / ingest.py 的解析步)额外依赖 ——
# 复用 swxy 项目环境；关键版本(见 HANDOFF C)：
#   onnxruntime-gpu==1.22  xgboost==2.1.4  nltk  opencv-python  shapely  pandas
# 纯问答/聚合服务无需这些；仅"新研报入库"链路需要。
